 <h1>Edit Wiki</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Wikis Rules</p>
     <ul>
       <li>Use Topics with debate issue.</li>
       <li>Use only appropiate language or it will be deleted.</li>
       <li>The incessant use of hashtags will get you banned.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @wiki do |f| %>
       <div class="form-group">
         <%= f.label :title %>
         <%= f.text_field :title, class: 'form-control', placeholder: "Enter wiki title" %>
       </div>
       <div class="form-group">
         <%= f.label :body %>
         <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter wiki body" %>
       </div><br/><br/>
     
            <% if  current_user.id == @wiki.user_id && current_user.role == "premium" || current_user.role == "admin" %>
                 <%= "Set To Private"%>
                 <%= f.radio_button(:private, true ) %>  <br/>
                 <%= "Set To Public"%>
                 <%= f.radio_button(:private, false ) %>  <br/><br/>
             <% end %>
     
       <div class="form-group">
         <%= f.submit "Save", class: 'btn btn-success' %>
       </div>
     <% end %>
   </div>
   <br/><br/>
   
   <% if current_user.role == 'premium' || current_user.role == 'admin'%>
 <%= render "collaborators/collaborators" %><br/><br/>
   <% end %>


